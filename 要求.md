1. iouring是 linux 操作系统上一种新的网络和文件系统的api；效率要高于  libaio。 
2. 基于C++开发一个 s3服务器。支持对象存储的GET/PUT和删除、对象列举功能。 
3. s3服务器要支持多线程；
4. S3服务器在读写本地文件时，使用iouring接口。 
5. 借此机会，全面学习一下linux  OS层面的 文件系统接口，包括 目录读写、文件读写等 api；
6. 接收和发送消息的缓冲区，可以使用我发给你的msg buffer。
7. 读目录，s3的v2版本认证。